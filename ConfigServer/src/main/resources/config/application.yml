spring:
  application:
    name: springboot
  datasource:
    url: jdbc:mysql://localhost:3308/hotel?characterEncoding=utf8&useSSL=false
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  mail:
    host: smtp.qq.com
    port: 465
    username: 2858712518@qq.com
    password: lzbticyvndvddecd
    properties:
      mail.smtp.auth: true
      mail.smtp.ssl.enable: true
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    consumer:
      key-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      auto-offset-reset: earliest
      properties:
        # 正确配置代理反序列化器类路径（注意属性名格式）
        spring.deserializer.key.delegate.class: org.apache.kafka.common.serialization.StringDeserializer
        spring.deserializer.value.delegate.class: org.springframework.kafka.support.serializer.JsonDeserializer
        # 信任的包路径
        spring.json.trusted.packages: "org.example.springboot.api.entity"
        # 启用类型头以支持多类型消息（可选）
        spring.json.use.type.headers: true
  data:
    redis:
      host: localhost
      port: 6379
  cache:
    type: redis
server:
  url: http://localhost:8080

jwt:
  secret: your-256-bit-secret-long-string-here-with-at-least-32-characters-long

mybatis-plus:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: org.example.springboot.api.entity
  configuration:
    map-underscore-to-camel-case: false
file:
  upload-dir: upload/files
