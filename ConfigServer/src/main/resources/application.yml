server:
  port: 8888

spring:
  profiles:
    active: native  # 激活 native 配置源
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          searchLocations: classpath:/config  # 指定配置文件的位置
